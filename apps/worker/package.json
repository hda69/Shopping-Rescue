{
  "name": "@shopping-rescue/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/server.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@shopping-rescue/database": "workspace:*",
    "@shopping-rescue/email": "workspace:*",
    "@shopping-rescue/rules-engine": "workspace:*",
    "@shopping-rescue/scanner": "workspace:*",
    "@shopping-rescue/shared": "workspace:*",
    "playwright": "^1.49.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
